# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

# Dependencies
node_modules
dist
dist-ssr
dist-lambda
dist-lambda.zip
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Environment variables
.env
.env.local
.env.production.local
.env.development.local

# Build artifacts
build
.cache

# SSL certificates (generated by vite-plugin-basic-ssl)
*.pem

# Testing
coverage
.nyc_output

# Misc
.netlify
# .claude/ is local-only, EXCEPT the committed 8th Wall engine-docs skill below.
.claude/*
!.claude/skills/
.claude/skills/*
!.claude/skills/8thwall-engine/

# Smoke-test artifacts (Playwright MCP)
.playwright-mcp/
desktop-mock-*.png

# Claude / superpowers / planning artifacts — local only, never pushed
docs/superpowers/
task_plan.md
findings.md
progress.md

# Handover and poster source material — circulated directly to the PI and the
# lab, never through the repo. These carry names, sign-off lines, and
# print-resolution artwork that have no reason to be in version control.
# Image patterns are root-anchored so public/ and src/ artwork stays tracked.
*.docx
*.doc
*.pptx
/*.jpg
/*.jpeg
/*.png